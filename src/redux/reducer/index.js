const initialState = {
  token: "",
  userList: [],
  currentUser: {},
};

function userReducer(state = initialState, action) {
  switch (action.type) {
    default:
      return state;
  }
}

export default userReducer;
